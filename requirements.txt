# AI-Powered Audit Analysis Dashboard — dependencies
# Pinned loosely for now; will freeze exact versions before final review.

streamlit>=1.40
google-genai>=1.0               # Gemini 2.0 Flash client (new SDK)
pymupdf>=1.24                    # PDF parsing (imported as "fitz")
python-docx>=1.1                 # DOCX parsing
pandas>=2.2                      # XLSX / CSV parsing
openpyxl>=3.1                    # pandas engine for .xlsx
plotly>=5.24                     # charts & risk matrices
pydantic>=2.9                    # AuditAnalysis schema validation
python-dotenv>=1.0              # local env var loading (dev convenience)
