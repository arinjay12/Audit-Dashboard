# Copy this file to "secrets.toml" (same folder) and paste your real key.
# secrets.toml is gitignored so the key never reaches GitHub.
# Streamlit Community Cloud reads the same keys from its Secrets UI later.

GEMINI_API_KEY = "your-google-ai-studio-key-here"
GEMINI_MODEL = "gemini-2.0-flash"
